package ${PACKAGE_NAME}.repository.${MK};

import ${PACKAGE_NAME}.entity.${MK}.${CLASS_NAME};
import com.jpa.IBaseDao;
/**
 * ${CLASS_NAME} 数据库操作类
 * Created by codeGeneration
 */
public interface ${CLASS_NAME}Dao extends IBaseDao<${CLASS_NAME}, ${PRI}> {
}