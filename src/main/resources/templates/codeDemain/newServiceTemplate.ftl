package ${PACKAGE_NAME}.service.${MK};

import ${PACKAGE_NAME}.entity.${MK}.${CLASS_NAME};
import com.si.service.IService;

/**
 * ${CLASS_NAME}业务接口类
 * Created by xiaoyaolan.
 */
public interface I${CLASS_NAME}Service extends IService<${CLASS_NAME}> {
}